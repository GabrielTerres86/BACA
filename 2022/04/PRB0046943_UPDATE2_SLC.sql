BEGIN
  UPDATE TBDOMIC_LIQTRANS_ARQUIVO
     SET NMARQUIVO_GERADO = NULL
        ,DHARQUIVO_GERADO = NULL
   WHERE TRUNC(DHRECEBIMENTO) = TRUNC(SYSDATE - 1)
     AND NMARQUIVO_GERADO IS NOT NULL;
  COMMIT;
END;
