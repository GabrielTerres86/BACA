DECLARE
  VR_CDCOOPER CRAPDEV.CDCOOPER%TYPE := 1;
  VR_NRDCONTA CRAPDEV.NRDCONTA%TYPE := 8457972;
  VR_NRCHEQUE CRAPDEV.NRCHEQUE%TYPE := 280;
BEGIN
  UPDATE CRAPDEV
  SET CDALINEA = 22
  WHERE CDCOOPER = VR_CDCOOPER
  AND NRDCONTA = VR_NRDCONTA
  AND NRCHEQUE = VR_NRCHEQUE;
  IF SQL%ROWCOUNT = 2 THEN
    COMMIT;
  ELSE
    ROLLBACK;
  END IF;
EXCEPTION
  WHEN OTHERS THEN
    CECRED.PC_INTERNAL_EXCEPTION;
    ROLLBACK;
END;
