CREATE OR REPLACE TRIGGER CECRED.TRG_CRAPSLR_NRSEQSOL
BEFORE INSERT ON CRAPSLR
FOR EACH ROW
BEGIN
  -- Buscar sequencia para PK da tabela caso não foi passada
  IF :new.nrseqsol IS NULL THEN
    SELECT CRAPSLR_NRSEQSOL.NEXTVAL INTO :NEW.NRSEQSOL FROM DUAL;
  END IF;
END;
/

