CREATE OR REPLACE TRIGGER CECRED.TRG_CRAPCSO_NRSEQSOL
BEFORE INSERT ON CRAPCSO
FOR EACH ROW
BEGIN
  -- Buscar sequencia para PK da tabela caso não foi passada
  IF :new.nrseqsol IS NULL THEN
    SELECT CRAPCSO_NRSEQSOL.NEXTVAL INTO :NEW.NRSEQSOL FROM DUAL;
  END IF;
END;
/

