CREATE OR REPLACE TRIGGER CECRED.TRG_CRAPSLA_NRSEQSOL
BEFORE INSERT ON CRAPSLA
FOR EACH ROW
BEGIN
  -- Buscar sequencia para PK da tabela caso não foi passada
  IF :new.nrseqsol IS NULL THEN
    SELECT CRAPSLA_NRSEQSOL.NEXTVAL INTO :NEW.NRSEQSOL FROM DUAL;
  END IF;
END;
/

