create trigger CECRED.TRGDOMIC_MENSAGEM_LIQ_SLC
before insert on CECRED.TBDOMIC_MENSAGEM_LIQ_SLC
for each row
BEGIN
  SELECT CECRED.SEQDOMIC_MENSAGEM_LIQ_SLC_ID.NEXTVAL
  INTO :NEW.idmensagem
  FROM DUAL;
END;